# Database (SQLAlchemy async)
DATABASE_URL=postgresql+asyncpg://linkagent:linkagent@localhost:5432/linkagent

# Database (LangGraph psycopg3 checkpointer)
CHECKPOINT_DATABASE_URL=postgresql://linkagent:linkagent@localhost:5432/linkagent

# LLM (uses Claude CLI — no API key needed, just `claude` installed + authenticated)
CLAUDE_MODEL=sonnet

# Image Generation
GEMINI_API_KEY=your-gemini-api-key

# CORS
CORS_ORIGINS=http://localhost:3000

# Frontend
NEXT_PUBLIC_API_URL=http://localhost:8000/api
